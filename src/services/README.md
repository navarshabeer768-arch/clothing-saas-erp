# services/

Frontend-side service modules go here in later phases (e.g.
`authService.ts`, `storeService.ts`) — thin wrappers around
`src/lib/apiClient.ts` calls to the trusted server endpoints. Empty in
Phase 1 since no endpoints exist yet to wrap.
