# features/

Feature-based modules (Products, Inventory, POS, Sales, ...) will each get
their own folder here in later phases, e.g. `features/products/`,
`features/pos/`, containing that module's components, hooks, and services.
Empty in Phase 1 by design (see project root README "Do not build yet").
